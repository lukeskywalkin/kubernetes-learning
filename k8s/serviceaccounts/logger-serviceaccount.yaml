apiVersion: v1
kind: ServiceAccount
metadata:
  name: logger-service-account
  namespace: default
  annotations:
    # In AWS EKS, this would be the IAM role ARN for IRSA
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/logger-role
    description: "Service account for logger service (IRSA pattern)"
automountServiceAccountToken: true
